sequence = "ACGT"
count = sequence.count ("A"), string.count("C"), string.count("G"), string.count("T")
count
# A very simple piece of code to count and return the number of bases within a DNA sequence back to me as integers
